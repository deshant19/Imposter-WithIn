# ============================================================
# Imposter Within — Backend Test Dependencies
# ============================================================
# Install with:
#   pip install -r requirements-test.txt
# ============================================================

# ── Runtime (pinned to same versions as requirements.txt) ────
fastapi==0.111.0
uvicorn[standard]==0.30.1
websockets==12.0
pydantic==2.7.1

# ── ASGI / Starlette (provides TestClient + WebSocket support)
starlette==0.37.2

# ── Test runner ───────────────────────────────────────────────
pytest==8.2.2

# ── Async test support ────────────────────────────────────────
pytest-asyncio==0.23.7
anyio[trio]==4.4.0

# ── HTTP test client (async + WebSocket) ─────────────────────
httpx==0.27.0

# ── Coverage ─────────────────────────────────────────────────
pytest-cov==5.0.0
coverage[toml]==7.5.3
